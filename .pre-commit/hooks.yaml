black:
    cmd: black
    flags:
    check: --check
ruff:
    cmd: ruff
    flags: --config /usr/share/pre-commit/ruff.toml --fix
    check: --config /usr/share/pre-commit/ruff.toml
mypy:
    cmd: mypy
    flags: --cache-dir=/tmp/mypy_cache --ignore-missing-imports --install-types --no-color-output --non-interactive
    check:
mdformat:
    cmd: mdformat
    flags: --wrap=88
    check: --check
